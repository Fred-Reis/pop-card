import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  card: {
    width: "100%",
    height: 200,
    marginVertical: 20,
  },

  number: {
    position: "absolute",
    bottom: 25,
    left: 80,
    color: "#3d3d3d",
    letterSpacing: 2,
    fontSize: 20,
  },
});
