import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  card: {
    width: "100%",
    height: 200,
    marginVertical: 20,
  },

  chip: {
    position: "absolute",
    width: 40,
    left: 100,
    top: 20,
  },

  number: {
    position: "absolute",
    bottom: 25,
    left: 100,
    color: "#ededed",
    letterSpacing: 2,
    fontSize: 20,
  },
});
