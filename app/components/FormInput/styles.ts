import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    backgroundColor: "#fff",
    fontSize: 18,
    padding: 10,
    borderRadius: 10,
    marginBottom: 3,
    width: 200,
    textAlign: "center",
    color: "#3e3e3e",
  },

  errorMessage: {
    marginBottom: 10,
    color: "#ec3030",
  },
});
