import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  title: {
    textAlign: "center",
    fontSize: 20,
    margin: 20,
    color: "#3d3d3d",
  },

  list: {
    padding: 20,
    borderRadius: 10,
    maxHeight: "60%",
  },
});
