import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#dddddd",
  },

  message: {
    color: "#3e3e3e",
    fontSize: 25,
    marginBottom: 25,
  },
});
