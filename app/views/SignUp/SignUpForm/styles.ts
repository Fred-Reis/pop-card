import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    borderColor: "#fff",
    borderWidth: 3,
    borderRadius: 10,

    padding: 10,
  },

  label: {
    marginTop: 10,
    color: "#3e3e3e",
  },

  buttonContainer: {
    alignSelf: "center",
    marginVertical: 20,
  },
});
