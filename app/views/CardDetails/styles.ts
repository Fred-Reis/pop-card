import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    paddingTop: 30,
    alignItems: "center",
    justifyContent: "center",
    backgroundColor: "#ddd",
  },

  title: {
    fontSize: 20,
    fontWeight: "500",
    color: "#3d3d3d",
  },

  message: {
    color: "#3e3e3e",
    fontSize: 25,
    marginBottom: 25,
  },
});
