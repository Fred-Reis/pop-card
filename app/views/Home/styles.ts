import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    backgroundColor: "#dddddd",
  },

  greating: {
    color: "#3e3e3e",
    fontSize: 18,
    marginBottom: 5,
    marginTop: 15,
    marginLeft: 25,
  },

  message: {
    color: "#3e3e3e",
    fontSize: 20,
    fontWeight: "500",
    marginBottom: 5,
    textAlign: "center",
  },
});
