import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    backgroundColor: "#dddddd",
  },

  message: {
    color: "#3e3e3e",
    fontSize: 25,
    marginBottom: 25,
    marginTop: 20,
    textAlign: "center",
  },
});
