import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
  },

  message: {
    color: "#fff",
    fontWeight: "bold",
    fontSize: 30,
    marginBottom: 25,
    textAlign: "center",
  },

  logo: {
    height: 230,
    marginBottom: 30,
  },
});
